The Iterator Pattern provides a way to access the elements of a collection sequentially without exposing its internal structure.
Simple words:
It lets you traverse a collection without knowing how the collection stores its data.
Think of like tv remote u press next channel

Iterator Pattern Motivation

Suppose you have a collection, such as an array or list, and you need to provide a mechanism for accessing its elements. Without the iterator pattern, the client code needs to understand how the collection is structured, and different collections would require different methods to traverse them.
Iterator Pattern - Simple Definition

You don't know
whether channels are stored in an array 
linked list 
database 
You only know
Next
That's exactly an Iterator.

                  +----------------------+
                  |      Iterator        |
                  +----------------------+
                  | + hasNext()          |
                  | + next()             |
                  +----------------------+
                             ▲
                             |
                  +----------------------+
                  |   StudentIterator    |
                  +----------------------+
                  | - students           |
                  | - index              |
                  +----------------------+
                  | + hasNext()          |
                  | + next()             |
                  +----------------------+
                             ▲
                             |
                    created by
                             |
                  +----------------------+
                  | StudentCollection    |
                  +----------------------+
                  | - students[]         |
                  +----------------------+
                  | + add()              |
                  | + createIterator()   |
                  +----------------------+

[10-07-2026 01:20 AM] Abhishek Kumar √∆Dπ: Why is this useful?

Suppose tomorrow you change the internal storage:

Today: students = []; Tomorrow:
students = new Map();
or
students = LinkedList;
Will the client change?
No
The client still writes:

const iterator = students.createIterator();
while (iterator.hasNext()) {
    console.log(iterator.next());
}
Only StudentIterator changes. This keeps the client independent of how the collection is implemented.

Iterator Pattern Benefits
1. Separation of Concerns: The traversal logic is separated from the collection itself, allowing you to change one without affecting the other.
2. Uniform Interface: The same interface (Iterator) is used to traverse different types of collections, making the code more flexible.
3. Simplified Client Code: The client doesn't need to know the underlying data structure, reducing coupling and making the code easier to maintain.
4. Multiple Traversal Strategies: You can implement multiple types of iterators (e.g., forward, backward, filtered) without changing the collection.
Multiple Iterators can traverse the same collection simultaneously without affecting each other.

Correct

The Iterator pattern allows for multiple iterators to operate on the same collection, enabling concurrent traversal without interference.